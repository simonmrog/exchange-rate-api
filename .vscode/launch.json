{
  "configurations": [
    {
      "name": "Python: Remote Attach",
      "type": "python",
      "request": "attach",
      "connect": { "host": "localhost", "port": 10000 },
      "pathMappings": [
        { "localRoot": "${workspaceFolder}", "remoteRoot": "/app/usr/src" }
      ]
    }
  ]
}
